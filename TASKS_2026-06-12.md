# רשימת משימות — יום שישי 12 ביוני 2026

**מטרה:** להפעיל את מערכת המשובים ב-Supabase, לבדוק מקצה לקצה, ולהשלים פרסום + משימות פתוחות מהפרויקט.

**תיעוד:** [TESTIMONIALS.md](TESTIMONIALS.md) · [SUPABASE_SETUP.md](SUPABASE_SETUP.md) · [PRD.md](PRD.md)

---

## היכן עצרנו — המשך מכאן (עדכון אחרון)

### מה כבר עובד
- טבלת `participant_feedback` ב-Supabase + תצוגה `participant_feedback_public` (VIEW, לא טבלה)
- טופס משוב (`tried-to-say.html`) — שליחה מקומית עובדת
- דף משובים (`testimonials.html`) — מציג משובים שפורסמו
- בדיקת דוא"ל מיידית מתחת לשדה + הודעות שגיאה ספציפיות (לא «לא מילאתם הכל»)
- אזהרת גיבריש + «שליחה בכל זאת»
- מודרציה: `status` בתפריט (`published` / `private` / `removed`) — אחרי `20260612_participant_feedback_status_enum.sql`
- הסרת משובי בדיקה מהאתר (`removed`)

### המשך מומלץ מחר — לפי סדר
1. **משימה 3** — פריסת `send-tried-to-say-email` + בדיקת מייל (אם עדיין לא)
2. **משימה 4** — השלמת בדיקות: `private`, אנונימי, מובייל
3. **משימה 6** — סימון רשימת QA שנותרה
4. **משימות 8–10** — GitHub push + Pages + QR לאירועים

### פקודת התחלה ב-Cursor מחר
> «בואי נמשיך מ-TASKS_2026-06-12.md — משימה 3, מייל למשובים»

---

## בוקר — Supabase ומשובים (≈ 1 שעה)

### 1. הרצת SQL — טבלת משובים (חובה)
- [x] היכנסי ל-[Supabase Dashboard](https://supabase.com/dashboard) → פרויקט **NuritSH_Site**
- [x] **SQL Editor** → **New query** (ראי הסבר בסוף המסמך)
- [x] העתיקי את **כל** התוכן מ-`supabase/migrations/20260611_participant_feedback.sql`
- [x] לחצי **Run**
- [x] ודאי: **Table Editor** → מופיעה טבלה `participant_feedback`
- [x] **בונוס:** `20260612_participant_feedback_status_enum.sql` — תפריט `status`

### 2. `supabase-config.js` (רק אם טופס יצירת קשר עדיין לא עובד)
- [x] `supabase-config.js` מוגדר (הטופס שולח)
- [ ] האתר חייב לרוץ דרך `http://localhost:8080` — לא `file://`

### 3. פריסת פונקציית מייל למשובים
- [ ] בטרמינל מהתיקייה של הפרויקט:
  ```powershell
  cd "C:\Users\Nurit SH\.cursor\projects\Nurits_site"
  npx supabase functions deploy send-tried-to-say-email
  ```
- [ ] אם מייל יצירת קשר כבר עובד — אין צורך בסודות חדשים (הפונקציה משתמשת ב-`CONTACT_EMAIL_WEBHOOK_URL`)
- [ ] אם מייל יצירת קשר **לא** עובד — הגדירי secrets לפי [SUPABASE_SETUP.md](SUPABASE_SETUP.md) §4

### 4. בדיקת טופס משוב מקומית
- [x] הריצי שרת: `python -m http.server 8080`
- [x] פתחי: `http://localhost:8080/tried-to-say.html?category=teachers`
- [x] שלחי משוב בדיקה עם **«כן, לפרסם»** + שם מלא
- [x] ודאי שורה חדשה ב-`participant_feedback` (Table Editor)
- [x] ודאי שהמשוב מופיע ב-`http://localhost:8080/testimonials.html#teachers`
- [ ] שלחי משוב שני עם **«לא לפרסום»** → שורה עם `status=private`, **לא** מופיע באתר
- [ ] בדקי מייל לנורית + מייל תודה למשתתף (אם webhook מוגדר)

### 5. מודרציה — מחיקה מהאתר
- [x] ב-Table Editor: שנהי `status` ל-`removed` על משוב בדיקה
- [x] רענני `testimonials.html` — המשוב נעלם מהאתר (השורה נשארת ב-DB)

---

## לפני הצהריים — בדיקות משובים (≈ 45 דק׳)

### 6. רשימת QA למשובים (מ-[TESTIMONIALS.md](TESTIMONIALS.md) §15)
- [ ] QR → טופס, קטגוריה מסומנת (`?category=…`)
- [ ] הסכמה «לא לפרסום» → לא באתר, כן במייל לנורית
- [ ] הסכמה «כן + שם» → מיד באתר עם שם
- [ ] הסכמה «כן + אנונימי» → מיד באתר כ«אנונימי»
- [ ] `spellcheck` + הודעת «קראו שוב» מופיעים
- [ ] מחיקה (`removed`) → נעלם מהאתר
- [ ] דוא"ל לא חשוף בדף ציבורי
- [ ] אין קישור ל-`tried-to-say.html` מ-`testimonials.html` או מהתפריט
- [ ] מובייל אחרי סריקת QR (סימולציה: צמצום חלון)

### 7. קישורי תצוגה מהדפים הקיימים
- [x] `lectures.html` → קישור ל-`testimonials.html#public`
- [x] `training.html` → קישור ל-`testimonials.html#teachers`
- [x] `apps.html` → קישור ל-`testimonials.html#apps`
- [x] כל הדפים — «משובים» בתפריט

---

## אחר הצהריים — פרסום ותשתית (≈ 1.5 שעות)

### 8. GitHub — העלאת קוד
- [ ] `git status` — ודאי שכל הקבצים החדשים נכללים
- [ ] commit + push (כשמוכן)
- [ ] **לא** להעלות סודות — `supabase-config.js` נשאר מקומי או anon בלבד ב-Pages

### 9. GitHub Pages
- [ ] Settings → Pages → Source: `main` / root
- [ ] ודאי ש-`supabase-config.js` קיים בפרודקשן (anon key מותר ציבורית)
- [ ] בדקי את האתר החי — דף משובים + ניווט

### 10. QR לאירועים
החליפי `…` בכתובת האתר החיה (למשל `https://nsh68.github.io/Nurits_site`):

| קטגוריה | כתובת ל-QR |
|---------|------------|
| קהל רחב | `…/tried-to-say.html?category=public-lecture` |
| מורים / הדרכות | `…/tried-to-say.html?category=teachers` |
| אפליקציות | `…/tried-to-say.html?category=app-development` |

- [ ] צרי 3 קודי QR (כלי חינמי או מדפסת)
- [ ] טקסט לשקף: «סרקו וספרו לנו — התנסינו ורצינו לומר»

---

## משימות פתוחות מהפרויקט (מ-[TASKS_2026-06-08.md](TASKS_2026-06-08.md))

### 11. טופס יצירת קשר — אם עדיין לא הושלם
- [ ] טופס `contact.html` שומר ב-`contact_inquiries`
- [ ] מסך תודה עם `assets/contact-thanks-clownfish.png`
- [ ] `supabase functions deploy send-contact-email` + webhook למייל

### 12. צ'אט Gemini (אופציונלי)
- [ ] `supabase secrets set GEMINI_API_KEY="..."`
- [ ] `npx supabase functions deploy chat`
- [ ] בדיקה ב-`contact.html`
- [ ] הוספת `chat-widget.js` לשאר הדפים (לפי מפרט)

### 13. בדיקות כלליות (QA)
- [ ] כל הדפים — טקסט, תמונות, קישורים
- [ ] מובייל (רוחב ≤ 900px)
- [ ] Chrome + Edge
- [ ] נגישות בסיסית — Tab, skip-link

### 14. ניקוי וסגירה
- [ ] עדכון `TESTIMONIALS.md` — סטטוס «מיושם» אחרי בדיקה מוצלחת
- [ ] עדכון `PRD.md` / `SESSION_SUMMARY.md`
- [ ] בדיקה אחרונה מהטלפון — אתר חי + טופס QR

---

## שלב 2 — תמונות קהל (לא דחוף לשישי)

- [ ] `upload-photos.html` — נורית + PREVIEW
- [ ] טבלה `audience_photos` + Storage `audience-photos`
- [ ] גלריה ב-`testimonials.html` (רק כשיש תמונות)
- [ ] עד 2 תמונות אופציונליות בטופס משתתף

---

## שלב עתידי — צ'אט (נושאים מוכנים)

- [ ] קבצי `topics/` — הרצאות, תמחור, הבהרת קהל (לפי תכנון קודם)
- [ ] חיבור ל-`chat-widget.js`

---

## סדר עדיפויות ליום שישי

| עדיפות | משימה | חובה? |
|--------|--------|-------|
| 🔴 1 | הרצת SQL + בדיקת טופס משוב | כן |
| 🔴 2 | פריסת `send-tried-to-say-email` | כן (למייל) |
| 🔴 3 | QA משובים (§6) | כן |
| 🟡 4 | QR לשלוש קטגוריות | מומלץ לפני אירוע |
| 🟡 5 | GitHub Pages + config | כן לפרודקשן |
| 🟢 6 | יצירת קשר / צ'אט / תמונות | לפי זמן |

---

## מה כבר מוכן בקוד — לא צריך לבנות מחדש

- `testimonials.html` + `testimonials.js` — תצוגה לפי קטגוריה
- `tried-to-say.html` + `tried-to-say.js` — טופס QR, הסכמות, spellcheck
- `supabase/migrations/20260611_participant_feedback.sql`
- `supabase/functions/send-tried-to-say-email/`
- ניווט «משובים» + קישורי תצוגה מ-lectures / training / apps
- עיצוב ב-`styles.css`

---

## התחלה מומלצת — המשך

פתחי Cursor וכתבי:

> «בואי נמשיך מ-TASKS_2026-06-12.md — משימה 3, מייל למשובים»

או:

> «בואי נשלים QA משובים ואז GitHub Pages»
